  para projeto:
  
  data = (uint8_t) data32;
  
  if (data == 0xE0) {
    expectingSecondByte = true;
  } else {
    if (expectingSecondByte) {
      bytes[1] = data;
      scancode = (bytes[0] << 8) | bytes[1];
      expectingSecondByte = false;
    } else {
      bytes[0] = data;
      scancode = data;
    }
    contadorBytes = (contadorBytes + 1) % 2;
  }